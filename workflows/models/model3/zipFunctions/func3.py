def augmenter(node):
    # TODO: use subprocesses to get original query and data from retriever
    #       and feed to ollama
    # print("Augmenter reception ->", node.getData())
    return node.getData()